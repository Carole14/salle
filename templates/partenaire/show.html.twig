{% extends 'base.html.twig' %}

{% block title %}Partenaire{% endblock %}

{% block body %}
    <h1>Partenaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ partenaire.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ partenaire.name }}</td>
            </tr>
            <tr>
                <th>Active</th>
                <td>{{ partenaire.active }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ partenaire.description }}</td>
            </tr>
            <tr>
                <th>Contact du technicien</th>
                <td>{{ partenaire.technicalContact }}</td>
            </tr>
            <tr>
                <th>Contact du commercial</th>
                <td>{{ partenaire.commercialContact }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_partenaire_index') }}">Retour à la liste des partenaires</a>

    <a href="{{ path('app_partenaire_edit', {'id': partenaire.id}) }}">edit</a>

    {{ include('partenaire/_delete_form.html.twig') }}
{% endblock %}
